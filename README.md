# Personal-Site
My personal WordPress site.

Note that the file wp-config.php contains sensitive database secrets, so it is not included in the repository. The Azure deployment adds it from a secure secrets repository.